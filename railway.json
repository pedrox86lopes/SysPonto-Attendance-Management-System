{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd attendance_system_django && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd attendance_system_django && python manage.py migrate && python manage.py collectstatic --noinput && python manage.py create_test_data && gunicorn attendance_system.asgi:application --bind 0.0.0.0:$PORT"
  }
}