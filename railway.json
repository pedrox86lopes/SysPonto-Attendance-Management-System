{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd attendance_system_django && python manage.py migrate && python manage.py collectstatic --noinput && python manage.py create_test_data && uvicorn attendance_system.asgi:application --host 0.0.0.0 --port $PORT"
  }
}